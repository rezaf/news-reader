<h2>Feed</h2>

<ul>
  <% feeds.each(function (feed) { %>
    <li><a href="#feeds/<%= feed.id %>" ><%= feed.get('title') %></a></li>
    <% feed.each(function (entry) { %>
      <li><%= entry.escape('title') %></li>
    <% }); %>
  <% }); %>
</ul>
